#include "lists.h"
/**
 * pop_listint? (- rm the head node)?
 *
 * @h: input head
 * Return: n
 */
int sum_listint(listint_t *h)
{
   
	listint_t *z, *t = h;

	if (h == NULL)
		return (0);
	n = 0;
	while (n < i)
	{
		h = h->next;
		n++;
	}
	z = h;
	z->n = h->n;
	z->next = h->next;
	return (z);
}
