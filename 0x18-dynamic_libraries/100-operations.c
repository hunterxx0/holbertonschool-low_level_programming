#include "holberton.h"
/**
* add(:)? (- add)?
*
* @a: input number
* @b: input number
* Return: a
*/
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub(:)? (- sub)?
 *
 * @a: input number
 * @b: input number
 * Return: s
 */
int sub(int a, int b)
{
        return (a - b);
}
/**
 * mul(:)? (- mul)?
 *
 * @a: input number
 * @b: input number
 * Return: m
 */
int mul(int a, int b)
{
        return (a * b);
}/**
  * div(:)? (- div)?
  *
  * @a: input number
  * @b: input number
  * Return: d
  */
int div(int a, int b)
{
        return (a / b);
}
/**
 * mod(:)? (- mod)?
 *
 * @a: input number
 * @b: input number
 * Return: m
 */
int mod(int a, int b)
{
        return (a % b);
}
